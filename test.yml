config:
  target: 'http://127.0.0.1:3000'
  http:
    #10 minutes threshold for request timeout 
    timeout: 600
  phases:
    - duration: 60
      arrivalRate: 10
  defaults:
    headers:
      x-my-service-auth: '987401838271002188298567'
scenarios:
  - flow:
    - get:
        url: "/startbrowsing/100"
